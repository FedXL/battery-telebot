version: '3.9'
services:
  bot-telegram:
    build:
        context: .
        dockerfile: Dockerfile.bot
    command: python run_bot.py
    volumes:
      - ./.env:/app/.env
      - ./aiogram-bot:/app

    networks:
      - battery_battery-net
networks:
  battery_battery-net:
    external: true